(ns knowtator.knowtator-xml-parser-test
  (:require [knowtator.knowtator-xml-parser :as sut]
            [clojure.test :refer [testing deftest is]]))

(deftest parse-annotation-file-test
  (testing "Basic parse annotation file"))
